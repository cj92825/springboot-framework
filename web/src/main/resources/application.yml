spring:
  profiles:
    active: dev
    include: orm,cache

logging:
  file: spring.log
  level:
    root: info

---
spring:
  profiles: dev
  application:
    name: springboot-framework
server:
  port: 10010

---
spring:
  profiles: prod
  application:
    name: springboot-framework
server:
  port: 10010